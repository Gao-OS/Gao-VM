name: gaovmd
description: GaoVM daemon control plane
version: 0.1.0
publish_to: none
environment:
  sdk: ^3.0.0
dependencies:
  gaovm_rpc:
    path: ../../libs/gaovm_rpc
dev_dependencies:
  test: ^1.25.0
